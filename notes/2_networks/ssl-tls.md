## Протоколы TLS/SSL

**SSL (Secure Sockets Layer)** и его преемник **TLS (Transport Layer Security)** — это криптографические протоколы, обеспечивающие безопасную передачу данных поверх транспортного уровня (обычно TCP). Они используются для защиты HTTP (HTTPS), электронной почты (SMTPS, IMAPS), VPN и других протоколов.

#### Основные задачи TLS/SSL:

1. **Аутентификация** – подтверждение подлинности сервера (и опционально клиента) с помощью цифровых сертификатов.
2. **Конфиденциальность** – шифрование данных, чтобы их нельзя было прочитать при перехвате.
3. **Целостность** – защита от модификации данных в пути (с помощью кодов аутентификации сообщений, MAC).

#### Как работает TLS (упрощённо):

1. **Рукопожатие (Handshake):**
    - Клиент и сервер согласовывают версию протокола, набор шифров (cipher suite).
    - Сервер отправляет свой цифровой сертификат (содержит публичный ключ).
    - Клиент проверяет сертификат (с помощью доверенных центров сертификации, CA).
    - Генерируется сеансовый ключ (обычно с использованием алгоритмов Диффи-Хеллмана или RSA) для симметричного шифрования.
2. **Переключение на защищённое соединение:** все дальнейшие данные шифруются сеансовым ключом.
3. **Завершение сеанса:** корректное закрытие соединения с предупреждением.

#### Основные версии:

- **SSL 2.0, 3.0** – устаревшие, небезопасные.
- **TLS 1.0, 1.1** – также устарели, не рекомендуются.
- **TLS 1.2** – широко используется, надёжен.
- **TLS 1.3** – современная версия, улучшенная производительность и безопасность (сокращённый handshake, удаление устаревших алгоритмов). В заголовках пакетов указывает версию 1.2, для большей скрытности, реальная версия зашифрована.

#### Компоненты:

- **Сертификаты X.509** – электронные документы, удостоверяющие принадлежность публичного ключа владельцу. Выдаются центрами сертификации (CA: Let's Encrypt, DigiCert, GlobalSign).
- **Цепочка доверия** – сертификат сервера подписан промежуточным CA, который подписан корневым CA (встроенным в доверенные корневые центры ОС или браузера).
- **Асимметричное шифрование** – используется в рукопожатии для безопасного обмена ключами.
- **Симметричное шифрование** – используется для данных сеанса (быстрее).
- **Хэш-функции** – для целостности (HMAC).

#### Где используется TLS/SSL:

- HTTPS (HTTP over TLS) – защищённый веб.
- FTPS – защищённый FTP.
- SMTPS, IMAPS, POP3S – защищённая почта.
- VPN (OpenVPN, WireGuard используют другие протоколы, но TLS тоже может применяться).
- Защита API (REST, GraphQL через HTTPS).


---

## Decryption TLS

Для расшифровки TLS пакетов, нужно сохранять ключи TLS сессий браузера, для этого нужно установить переменную окружения `SSLKEYLOGFILE`, в этом случае библиотеки, которые используют браузеры (NSS, OpenSSL, boringssl) будут сохранять ключи сессий в указанный в переменной файл.

Windows:

- Нажмите комбинацию клавиш **Win + R**, введите `sysdm.cpl` и нажмите Enter.
- Перейдите на вкладку **«Дополнительно»** и нажмите кнопку **«Переменные среды»**. 
- В верхнем блоке («Переменные среды для пользователя») нажмите **«Создать»**.
- Заполните поля:
    - **Имя переменной:** `SSLKEYLOGFILE`
    - **Значение переменной:** Укажите полный путь к файлу (например, `C:\Users\Username\Documents\sslkey.log`).
- Перезапустите браузер, чтобы он начал записывать ключи


Linux / macOS:

```bash
export SSLKEYLOGFILE=~/sslkey.log
```

**Чтобы переменная не сбрасывалась при закрытии терминала:**

1. Откройте файл профиля (например, `~/.bashrc` для Bash или `~/.zshrc` для Zsh).
2. Добавьте в самый конец файла строку с экспортом.
3. Сохраните изменения и выполните команду `source ~/.bashrc`


---

### Установка соединения TLS

- Набор шифров TLS
	- Алгоритм цифровой подписи в сертификате
	- Алгоритм обмена ключами
	- Алгоритм симметричного шифрования
	- Хэш-функция для вычисления MAC (код подтверждения целостности сообщения)
- Проверка подлинности сервера (и клиента например в mTLS)
- Обмен ключами симметричного шифрования
- Для соединения в TLS используется отдельный протокол handshake protocol

![[Pasted image 20260601135752.png]]

Когда устанавливать соединение с нуля не требуется, например клиент сделал запрос на сайт в браузере и отошел на несколько минут, используется 0-RTT соединение, в котором используется симметричный ключ прошлой сессии.

![[Pasted image 20260601140340.png]]


### [[wireshark#Установка TLS соединения|Установка соединения в wireshark]]
#### Разрыв соединения:

- Корректный разрыв соединения:
	- Отправка оповещения close_notify, закрытие соединения на запись
	- Должны отправить обе стороны
- Разрыв соединения в случае ошибки
	- Используются разные типы сообщений, соединение разрывается сразу

