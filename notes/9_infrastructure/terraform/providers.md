### Providers (Провайдеры)

Провайдеры — это «переводчики, обстракция» между кодом Terraform и API конкретного сервиса. Сам Terraform ничего не знает о том, как создать сервер в Yandex Cloud или базу данных в AWS.

- **Как это работает:** Когда вы пишете код, провайдер скачивается как отдельный бинарный файл (`terraform init`). Он принимает команды от Terraform и отправляет соответствующие запросы в облако.
- **Типы:** Есть официальные (от HashiCorp), партнерские (от самих облаков) и комьюнити-провайдеры (для экзотических сервисов).
- **Зачем:** Позволяют управлять всем из одного места — от «железа» в облаке до пользователей в GitHub или Cloudflare.

После перехода с одного провайдера на дргугого, начинает казаться что изучаешь язык программирования с нуля, поэтому нужно учить концепции, а не синтаксис: они одинаковы у всех: `resource "type" "name" { parametres }`. Разница только в именах полей, которые нужно подглядывать в документации. Под рукой всегда нужно держать [Terraform Registry](https://registry.terraform.io/) для поиска нужного провайдера.

Главные провайдеры, основы которых нужно знать в первую очередь:

- **[Yandex cloud](https://yandex.cloud/ru/docs/terraform/resources/kubernetes_cluster)**, **[AWS](https://registry.terraform.io/providers/hashicorp/aws/latest/docs)** или другое основное облако
- **[Kubernetes](https://registry.terraform.io/providers/hashicorp/kubernetes/latest)**, **[Helm_release](https://registry.terraform.io/providers/hashicorp/helm/latest/docs/resources/release)** для управления манифестами и чартами внутри кластера.
- `github` или `gitlab` для управления репозиториями, ключами и правами доступа.
- `random`, `tls`, `time` для генерации паролей, сертификатов и временных задержек.

---

Мне пока довелось работать с yc-provider, helm-release и proxmox(bpg). При переходе с облачного провайдера (**Cloud-API**) где облако само управляет дисками, сетями и IP, к **Bare-metal API** где нужно вручную управлять ID виртуалок, привязкой к конкретной ноде proxmox, ISO-образами, cloud-init шаблонами, синтаксис не меняется, но меняется физика процессов.

При смене провайдера необходимо первым делом:

1. **Изучить схему авторизации:** Облака используют токены/IAM-роли. Proxmox требует URL ноды, API Token ID (`user@pve!token_name`) и Secret, а также отключение проверки SSL (`insecure = true`), если сертификат самоподписанный.
2. **Понять логику именования ресурсов:** В облаке не надо думать об ID машины. В Proxmox `vmid` (например, `101`) часто является обязательным или критически важным для импорта.

[[examples|Примеры написанных IaC на terraform]]


Если созданный ресурс работает некорректно, как у меня это было с созданием LXC-контейнера через terraform, нужно попробовать добавлять доп аргументы из документации, которые казалось бы могут подтянуться автоматически, но у меня из за этого была ошибка контейнера. Ошибки от terraform не было, но контейнер не работал, пока я не указал список dns серверов, он не работал корректно. 

При работе с terraform, лучший друг это документация, так как технологии постоянно меняются, меняется синтаксис ресурсов, нейронка в этих вопросах очень часто ошибается и не может создать даже простые вещи. Поэтому провайдеров нужно изучать глядя в документацию, а именно, найти ресурсы, которые будут нужны для задачи, обычно это 3-4, и смотреть на их атрибуты. Основные ресурсы провайдеров:



### 1. Proxmox Provider (`bpg/proxmox`)

_Основная суть:_ Клонирование из шаблонов (Cloud-Init) или создание ВМ с нуля на конкретных физических серверах.

Основные ресурсы для управления инфраструктурой

| Ресурс                                          | Краткое описание                                                                                                                     |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| **`proxmox_virtual_environment_vm`**            | Управление виртуальными машинами (QEMU/KVM): создание, клонирование, настройка CPU, памяти, дисков, сетевых интерфейсов, cloud-init  |
| **`proxmox_virtual_environment_container`**     | Управление LXC-контейнерами. Позволяет создавать и настраивать контейнеры с указанием шаблона, ресурсов, сети и переменных окружения |
| **`proxmox_virtual_environment_download_file`** | Скачивание файлов (например, облачных образов `.qcow2`) на ноду Proxmox для использования в качестве шаблонов.                       |
| **`proxmox_virtual_environment_file`**          | Загрузка или управление пользовательскими файлами (например, конфигурации или скрипты) в хранилище Proxmox.                          |
| **`proxmox_virtual_environment_haresource`**    | Управление ресурсами в High Availability (HA) кластере Proxmox. Позволяет управлять состоянием HA-ресурсов                           |

Ресурсы для настройки сети и безопасности

| Ресурс                                                            | Краткое описание                                                                                         |
| ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| **`proxmox_virtual_environment_hosts`**                           | Управление файлом `/etc/hosts` на нодах Proxmox. Позволяет добавлять или удалять статические записи DNS. |
| **`proxmox_virtual_environment_firewall_rules`**                  | Управление правилами межсетевого экрана (firewall) для кластера или отдельных нод/машин.                 |
| **`proxmox_virtual_environment_cluster_firewall_security_group`** | Управление группами безопасности для firewall в рамках кластера.                                         |

Ресурсы для администрирования

| Ресурс                                 | Краткое описание                                                                                |
| -------------------------------------- | ----------------------------------------------------------------------------------------------- |
| **`proxmox_virtual_environment_pool`** | Управление пулами ресурсов для группировки и организации ВМ и контейнеров.                      |
| **`proxmox_virtual_environment_user`** | Управление пользователями в Proxmox VE, включая права доступа.                                  |
| **`proxmox_virtual_environment_role`** | Управление ролями для системы безопасности, которые определяют права доступа для пользователей. |

Ресурсы для мониторинга и расширенных настроек

| Ресурс                                 | Краткое описание                                                                                      |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **`proxmox_metrics_server`**           | Настройка отправки метрик Proxmox во внешние системы, такие как InfluxDB, Graphite или OpenTelemetry. |
| **`proxmox_virtual_environment_dns`**  | Управление DNS-настройками ноды.                                                                      |
| **`proxmox_virtual_environment_time`** | Управление настройками времени (NTP) на ноде Proxmox.                                                 |



### 2. Yandex Cloud Provider (`yandex-cloud/yandex`)

_Основная суть:_ Управление абстрактными виртуальными ресурсами в дата-центрах Яндекса.

Вычислительные ресурсы (Compute)

| Ресурс                               | Краткое описание                                                                                                         |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| **`yandex_compute_instance`**        | Создание и управление виртуальными машинами (ВМ): CPU, RAM, диски, сетевые интерфейсы, метаданные (например, SSH-ключи). |
| **`yandex_compute_disk`**            | Управление дисками: создание, изменение размера, подключение к ВМ.                                                       |
| **`yandex_compute_image`**           | Управление образами ОС (пользовательскими и публичными), которые используются для загрузки ВМ.                           |
| **`yandex_compute_snapshot`**        | Создание и управление снапшотами (снимками) дисков для бэкапов и восстановления данных.                                  |
| **`yandex_compute_instance_group`**  | Управление группами ВМ (Instance Groups) для автоматического масштабирования и управления парком машин.                  |
| **`yandex_compute_placement_group`** | Управление группами размещения (placement groups), которые влияют на физическое расположение ВМ в дата-центре.           |

Сеть и безопасность (VPC & Security)

| Ресурс                            | Краткое описание                                                                                                  |
| --------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| **`yandex_vpc_network`**          | Создание и управление облачными сетями (VPC).                                                                     |
| **`yandex_vpc_subnet`**           | Создание подсетей внутри облачных сетей с указанием зоны доступности и CIDR-блоков.                               |
| **`yandex_vpc_address`**          | Выделение статических публичных IP-адресов для ВМ или балансировщиков.                                            |
| **`yandex_vpc_security_group`**   | Управление правилами firewall (Security Groups), которые контролируют входящий и исходящий трафик на уровне сети. |
| **`yandex_vpc_private_endpoint`** | Создание приватных эндпоинтов для доступа к сервисам Yandex Cloud из вашей VPC (без публичного IP).               |

Управление доступом (IAM & Resource Manager)

| Ресурс                                                  | Краткое описание                                                                                                           |
| ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| **`yandex_iam_service_account`**                        | Создание и управление сервисными аккаунтами, от имени которых работают ВМ и другие сервисы.                                |
| **`yandex_resourcemanager_folder_iam_member`**          | Назначение ролей (например, `editor`, `storage.editor`) сервисным аккаунтам или пользователям на уровне каталога (folder). |
| **`yandex_iam_service_account_static_access_key`**      | Создание статических ключей доступа (Access/Secret Key) для сервисных аккаунтов (например, для работы с Object Storage).   |
| **`yandex_iam_workload_identity_federated_credential`** | Управление федерацией удостоверений для Workload Identity.                                                                 |
| **`yandex_organizationmanager_os_login_settings`**      | Настройка OS Login для организации.                                                                                        |
| **`yandex_organizationmanager_user_ssh_key`**           | Управление SSH-ключами пользователей организации.                                                                          |

Управляемые базы данных (Managed Databases)

| Ресурс                              | Краткое описание                                                   |
| ----------------------------------- | ------------------------------------------------------------------ |
| **`yandex_mdb_postgresql_cluster`** | Создание и управление кластерами Managed PostgreSQL.               |
| **`yandex_mdb_mysql_cluster`**      | Создание и управление кластерами Managed MySQL.                    |
| **`yandex_mdb_redis_cluster`**      | Создание и управление кластерами Managed Redis (включая sharding). |
| **`yandex_mdb_mongodb_cluster`**    | Создание и управление кластерами Managed MongoDB.                  |
| **`yandex_mdb_kafka_cluster`**      | Создание и управление кластерами Managed Apache Kafka.             |
| **`yandex_mdb_opensearch_cluster`** | Создание и управление кластерами Managed OpenSearch.               |

Объектное хранилище (Object Storage)

| Ресурс                      | Краткое описание                                 |
| --------------------------- | ------------------------------------------------ |
| **`yandex_storage_bucket`** | Создание и управление бакетами в Object Storage. |

Managed Kubernetes (MK8S)

| Ресурс                             | Краткое описание                                                      |
| ---------------------------------- | --------------------------------------------------------------------- |
| **`yandex_kubernetes_cluster`**    | Создание и управление кластером Managed Kubernetes.                   |
| **`yandex_kubernetes_node_group`** | Создание и управление группами узлов (node groups) для кластера MK8S. |

Application Load Balancer (ALB)

| Ресурс                         | Краткое описание                                                      |
| ------------------------------ | --------------------------------------------------------------------- |
| **`yandex_alb_load_balancer`** | Создание и управление L7-балансировщиком (Application Load Balancer). |
| **`yandex_alb_http_router`**   | Управление HTTP-роутерами для ALB.                                    |
| **`yandex_alb_backend_group`** | Управление бэкенд-группами для ALB.                                   |
| **`yandex_alb_virtual_host`**  | Настройка виртуальных хостов для ALB.                                 |

DNS и сертификаты (DNS & TLS)

| Ресурс                      | Краткое описание                                                                           |
| --------------------------- | ------------------------------------------------------------------------------------------ |
| **`yandex_dns_zone`**       | Создание и управление публичными или приватными DNS-зонами.                                |
| **`yandex_dns_recordset`**  | Создание записей (A, CNAME, MX) внутри DNS-зоны.                                           |
| **`yandex_cm_certificate`** | Управление TLS-сертификатами (например, импорт собственных или заказ через Let's Encrypt). |


### 3. Kubernetes Provider (`hashicorp/kubernetes`)

_Основная суть:_ Управление базовыми объектами оркестратора. На вход принимает Kubeconfig.

- **`kubernetes_namespace`** — Управление пространствами имен.
- **`kubernetes_deployment`** — Управление развертываниями (Deployments).
- **`kubernetes_service`** / **`kubernetes_service_v1`** — Управление сервисами для внутреннего доступа (ClusterIP, NodePort, LoadBalancer).
- **`kubernetes_ingress`** / **`kubernetes_ingress_v1`** — Управление правилами для входящего HTTP(S) трафика.
- **`kubernetes_secret`** — Управление секретами (например, для хранения паролей или credentials для pull-образов).
- **`kubernetes_config_map`** — Управление ConfigMap для хранения конфигурации приложений.
- **`kubernetes_stateful_set`** — Управление StatefulSet для stateful-приложений.
- **`kubernetes_job`** / **`kubernetes_cron_job`** — Управление заданиями (Jobs) и заданиями по расписанию.
- **`kubernetes_service_account`** — Управление сервисными аккаунтами для подов.
- **`kubernetes_role`** / **`kubernetes_role_binding`** / **`kubernetes_cluster_role`** / **`kubernetes_cluster_role_binding`** — Управление правами доступа (RBAC).
- **`kubernetes_persistent_volume`** / **`kubernetes_persistent_volume_claim`** — Управление томами для постоянного хранения данных.
- **`kubernetes_manifest`** — Универсальный ресурс для управления **любыми** объектами, включая **Custom Resources (CRDs)**, с использованием Server-Side Apply.


### 4. Helm Provider (`hashicorp/helm`)

_Основная суть:_ Управление высокоуровневыми пакетами (чартами) в Kubernetes. Самый лаконичный провайдер.

- `helm_release` — практически единственный ресурс, который вам нужен.
- _Что помнить:_ Передавайте параметры через блок `set { name = "..."; value = "..." }` или через `values = [ file("values.yaml") ]`. Всегда явно указывайте `depends_on = [kubernetes_namespace.example]`, чтобы Helm не пытался ставить чарт в еще не созданное пространство имен.

